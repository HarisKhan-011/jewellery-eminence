import CheckoutMainArea from "@components/checkout/checkout-main";

export const metadata = {
  title: "Checkout - Eminence Jewellery",
};

const Checkout = () => {
  return (
    <CheckoutMainArea/>
  );
};

export default Checkout;
